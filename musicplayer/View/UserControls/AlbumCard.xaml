<UserControl x:Class="musicplayer.View.UserControls.AlbumCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:musicplayer.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="60*"/>
        </Grid.RowDefinitions>

        <!-- Album Information -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70*"/>
                <ColumnDefinition Width="Auto"  />
            </Grid.ColumnDefinitions>

            <Border Name="AlbumInfoBorder"
                Grid.Row="0"
                Grid.Column="0"
                Style="{StaticResource CardStyle}"
                PreviewMouseDown="AlbumInfoBorder_PreviewMouseDown">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Name="SystemDataText"
                               Grid.RowSpan="3"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Center"
                               TextAlignment="Right"
                               Foreground="#3A4444"
                               Opacity="0.30"
                               FontSize="18"
                               FontFamily="Consolas"
                               TextWrapping="Wrap"
                               LineHeight="17"
                               IsHitTestVisible="False"
                               Panel.ZIndex="0"/>

                    <Grid Grid.Row="0"
                          Panel.ZIndex="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0"
                            Margin="0,0,20,0">

                            <TextBlock Name="AlbumTitle"
                               FontSize="40"
                               VerticalAlignment="Top"
                               HorizontalAlignment="Left"
                               TextWrapping="Wrap"
                               TextTrimming="CharacterEllipsis"
                               LineHeight="42"
                               MouseLeftButtonDown="AlbumTitle_MouseLeftButtonDown"/>

                            <TextBox Name="AlbumTitleEdit"
                                 Visibility="Collapsed"
                                 FontSize="40"
                                 Foreground="White"
                                 Background="#202124"
                                 BorderBrush="DarkSlateGray"
                                 BorderThickness="1"
                                 Padding="4,0"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="False"
                                 KeyDown="AlbumTitleEdit_KeyDown"
                                 LostFocus="AlbumTitleEdit_LostFocus"/>
                        </Grid>

                        <StackPanel Grid.Row="1"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Left">

                            <TextBlock Name="ArtistName"
                                       FontSize="25"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"/>

                            <TextBlock Name="ReleaseDate"
                                       FontSize="20"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Margin="35,0,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Left">

                            <TextBlock Name="NumSongs"
                                       FontSize="15"
                                       Foreground="#636363"
                                       HorizontalAlignment="Left"/>

                            <TextBlock Name="RunTime"
                                       FontSize="15"
                                       Foreground="#636363"
                                       HorizontalAlignment="Left"
                                       Margin="55,0,0,0"/>
                        </StackPanel>
                    </Grid>
                    <Button Grid.Row="1"
                        Name="AlbumInfoPlayButton"
                        Width="54"
                        Height="54"
                        Padding="12"
                        Margin="0,12,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Click="AlbumInfoPlayButton_Click"
                        Style="{StaticResource PlayerButtonStyle}"
                        ToolTip="Play album">

                        <Image Source="/assets/icons/play-256.png"
                        Stretch="Uniform"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Album Art -->
            <Border Name="AlbumCoverBorder"
                    Grid.Column="1"
                    Margin="10"
                    ClipToBounds="True">

                <Image Name="AlbumCover"
                   Cursor="Hand"
                   Stretch="UniformToFill"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Top"
                   MouseLeftButtonDown="AlbumCover_MouseLeftButtonDown"/>
            </Border>
        </Grid>

        <!-- Songs -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <ListView Name="Songs"
              Grid.Row="1"
              Grid.Column="0"
              FontSize="20"
              Grid.ColumnSpan="2"
              Margin="8,8,0,8"
              BorderThickness="0"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
              ScrollViewer.CanContentScroll="True"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              VirtualizingPanel.ScrollUnit="Pixel"
              PreviewMouseWheel="Songs_PreviewMouseWheel"
              ScrollViewer.PanningMode="VerticalOnly"
              Background="Transparent"
              Foreground="White">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem"
                        BasedOn="{StaticResource MusicListViewItemStyle}">

                        <Setter Property="Cursor" Value="Hand"/>
                        
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                <Setter Property="Background" Value="#2A2B2E"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="42"
                          Background="Transparent"
                          PreviewMouseLeftButtonDown="SongRow_PreviewMouseLeftButtonDown"
                          PreviewMouseDown="SongRow_PreviewMouseDown"
                          PreviewMouseRightButtonDown="SongRow_PreviewMouseRightButtonDown">



                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="58"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="34"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                               Text="{Binding TrackNumber}"
                               Foreground="White"
                               TextAlignment="Right"
                               VerticalAlignment="Center"
                               Margin="0,0,18,0"/>

                            <TextBlock Grid.Column="1"
                               Text="{Binding Title}"
                               Foreground="White"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Column="2"
                               Text="{Binding Duration}"
                               Foreground="White"
                               VerticalAlignment="Center"
                               Margin="30,0,8,0"
                               HorizontalAlignment="Right"/>

                            <Button Grid.Column="3"
                            Width="26"
                            Height="26"
                            Padding="3"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Focusable="False"
                            PreviewMouseLeftButtonDown="LikeButton_PreviewMouseLeftButtonDown">

                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                        <Setter Property="Focusable" Value="False"/>

                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="ButtonBorder"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="5"
                                                    Padding="{TemplateBinding Padding}">

                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                                    </Border>

                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="ButtonBorder"
                                                            Property="Background"
                                                            Value="#202124"/>
                                                        </Trigger>

                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="ButtonBorder"
                                                            Property="Background"
                                                            Value="#2A2B2E"/>
                                                        </Trigger>

                                                        <DataTrigger Binding="{Binding IsLiked}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>

                                <Image Source="{Binding LikeIconPath}"
                               Stretch="Uniform"/>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</UserControl>